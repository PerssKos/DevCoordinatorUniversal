name: coordinator_client
description: Typed, fail-closed DevCoordinator domain contracts and HTTP clients.
version: 0.2.0
publish_to: none

environment:
  sdk: ^3.12.2

resolution: workspace

dependencies:
  http: ^1.5.0

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.26.3
