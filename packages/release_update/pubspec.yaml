name: release_update
description: GitHub release discovery, conditional caching, and update policy.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

resolution: workspace

dependencies:
  http: ^1.6.0
  pub_semver: ^2.2.0

dev_dependencies:
  test: ^1.31.0
